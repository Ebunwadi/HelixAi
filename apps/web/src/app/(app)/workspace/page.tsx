export default function WorkspacePage() {
  return (
    <section>
      <p className="eyebrow">AI Workspace</p>
      <h2 className="page-title">The model layer starts in Sprint 3.</h2>
      <p className="page-lead">
        Sprint 2 deliberately establishes identity, tenancy and persistence first so future AI
        calls inherit the same security boundaries as the rest of the application.
      </p>
    </section>
  );
}
