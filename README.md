# HelixAI

Agentic customer operations platform.

This is a monorepo:

- `apps/api` — Python API, managed with `uv`
- `apps/web` — Next.js + TypeScript frontend

Full local setup and contribution instructions will be added before Sprint 1 is complete.
